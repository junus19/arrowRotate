namespace ArrowJam
{
    // Order is persistent — saved levels store these values. Only append, never reorder.
    public enum SnakeColor
    {
        Yellow,
        Red,
        Blue,
        Purple,
        Green,
        Orange,
        Cyan,
        Pink,
        Brown,
        White
    }
}
